import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

// shadcn AspectRatio (Radix). Держит заданное соотношение сторон у контейнера
// с абсолютным содержимым — превью камеры на экране проверки устройств.
const AspectRatio = AspectRatioPrimitive.Root;

export { AspectRatio };
